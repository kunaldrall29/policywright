# RECONCILIATION

Spike assumptions vs. verified reality. Sources and dates for every "actual"
column entry live in [FACTS.md](FACTS.md); OZ file:line references are within
`OpenZeppelin/stellar-contracts` @ **v0.7.2**
(`a9c42169000638da937577f592ebf61a7a3c94ca`). Compiled 2026-08-03.

**Every row marked ✗ is a required change in Prompts 2–3. Where the spike and
the actual OZ package disagree, the actual package wins — always.**

Rows cover: the trait name, every lifecycle method, the storage pattern, every
stock policy and its params, plus the context-rule, event-shape, toolchain, and
venue assumptions the spike baked in.

## The trait and its lifecycle

| #   | Spike assumption                                       | Actual OZ reality                                                                                                                                                                                                                                                                                      | Impact                                                                                                                                                                                                           | OZ source                                    |
| --- | ------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------- |
| 1   | Trait is named `Policy`                                | ✓ Confirmed: `pub trait Policy`                                                                                                                                                                                                                                                                        | None.                                                                                                                                                                                                            | `packages/accounts/src/policies/mod.rs:47`   |
| 2   | Lifecycle includes a **`can_enforce`** validation hook | ✗ **No `can_enforce` exists.** Lifecycle is exactly `install` / `enforce` / `uninstall`; validation AND state mutation both happen in `enforce`, which rejects by panicking                                                                                                                            | Any generated policy or docs describing a separate validate step is wrong; there is no read-only pre-check surface. (The current `src/rust-policy.ts` emitter already models this correctly — keep it that way.) | `mod.rs:47-160`; guideline at `mod.rs:44-46` |
| 3   | `install(params)` shape                                | ✓ `fn install(e, install_params: Self::AccountParams, context_rule: ContextRule, smart_account: Address)` — params typed via associated `type AccountParams: FromVal<Env, Val>`                                                                                                                        | Generated policies must declare `AccountParams`; install params arrive at `add_policy` time as a `Val`.                                                                                                          | `mod.rs:115-121`                             |
| 4   | `enforce(context)` shape                               | ✓ `fn enforce(e, context: Context, authenticated_signers: Vec<Signer>, context_rule: ContextRule, smart_account: Address)`; must be smart-account-authorized; panics to reject                                                                                                                         | Function/argument narrowing is possible here (`ContractContext.fn_name/args`), and only here.                                                                                                                    | `mod.rs:79-85`                               |
| 5   | `uninstall` shape                                      | ✓ `fn uninstall(e, context_rule: ContextRule, smart_account: Address)`; must panic if not installed                                                                                                                                                                                                    | Generated `uninstall` must check installed-ness, not silently no-op. Current generated Rust removes without checking — adjust in Prompt 2–3.                                                                     | `mod.rs:123-160`                             |
| 6   | Policies are called through the trait directly         | ✓/nuance: the smart account calls policies via a generated `PolicyClient` from a parallel private `PolicyClientInterface` (associated types unsupported by `#[contractclient]`); `uninstall` is invoked via `try_uninstall` (a panicking uninstall rolls back its event but the rule removal proceeds) | Doc-level nuance only.                                                                                                                                                                                           | `mod.rs:163-185`; `smart_account` docs       |

## Storage / segregation pattern

| #   | Spike assumption                                      | Actual OZ reality                                                                                                                                                                                                                                      | Impact                                                                                                                                                                                                                               | OZ source                                                                                   |
| --- | ----------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------- |
| 7   | Per-(smart account, context rule) storage segregation | ✓ Confirmed and uniform: every stock policy keys ALL state on `<Policy>StorageKey::AccountContext(Address, u32)` in **persistent** storage; `install` panics `AlreadyInstalled` on key collision; `enforce` begins with `smart_account.require_auth()` | The generated `FrequencyLimitPolicy`'s `DataKey::Params/Calls(Address, u32)` matches the pattern. Missing pieces to adopt: TTL management (`DAY_IN_LEDGERS = 17280`, extend 30 days, threshold 29) and the `AlreadyInstalled` guard. | `simple_threshold.rs:120-124`; `spending_limit.rs:144-149`; `weighted_threshold.rs:153-160` |

**D1.3 status (2026-08-03).** The generated-Rust consequences of rows 5 and 7
(and summary item 3) are implemented and compiled: the template now lives as a
real crate at [contracts/frequency-limit-policy](../contracts/) implementing
the v0.7.2 trait verbatim — `AlreadyInstalled` install guard,
panic-if-not-installed `uninstall` (`FrequencyLimitError::SmartAccountNotInstalled`,
error range 3230+ following the stock per-policy ranges), single
`AccountContext(Address, u32)` persistent key holding a
`SpendingLimitData`-style struct, stock TTL constants (extend 30 days /
threshold 29, applied in the getter exactly like the stock policies), and
`smart_account.require_auth()` opening install/enforce/uninstall. The emitter
output IS that crate source (byte-equality locked in
[test/rust-policy.test.ts](../test/rust-policy.test.ts)); 25 Rust tests cover
window rollover, count limit, per-(account, rule) isolation both directions,
and the uninstall lifecycle. Deliberate divergence from stock
`spending_limit`, documented in the crate docs: `enforce` does not require
non-empty `authenticated_signers` (frequency meters calls, not signers, and
the synthesizer attaches this policy to otherwise-empty token rules).

## Stock policies and their params

| #   | Spike assumption                                                                                             | Actual OZ reality                                                                                                                                                                                                                                                                                                                                                | Impact                                                                                                                                                                                                                                                                                                      | OZ source                                                                                        |
| --- | ------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ |
| 8   | Stock policies exist: `spending_limit`, `simple_threshold`, `weighted_threshold`                             | ✓ All three exist — but as **free-function modules**, not deployable contracts; a deployable policy is a wrapper contract implementing `Policy` by delegation (reference wrappers in OZ `examples/`)                                                                                                                                                             | "Compose-first" at Tranche level means: deploy (or reuse) thin wrapper contracts; the wrapper address is what goes in `ContextRule.policies`.                                                                                                                                                               | `policies/mod.rs:12-16`; `examples/multisig-smart-account/spending-limit-policy/src/contract.rs` |
| 9   | `simple_threshold` params                                                                                    | ✓ `SimpleThresholdAccountParams { threshold: u32 }` (min signers required)                                                                                                                                                                                                                                                                                       | None for T1 (unused).                                                                                                                                                                                                                                                                                       | `simple_threshold.rs:97-105`                                                                     |
| 10  | `weighted_threshold` params                                                                                  | ✓ `WeightedThresholdAccountParams { signer_weights: Map<Signer, u32>, threshold: u32 }`                                                                                                                                                                                                                                                                          | None for T1 (unused).                                                                                                                                                                                                                                                                                       | `weighted_threshold.rs:126-135`                                                                  |
| 11  | Spending limit is configured with a **time window in seconds** (repo emits `windowSecs`)                     | ✗ `SpendingLimitAccountParams { spending_limit: i128, period_ledgers: u32 }` — the rolling window is in **ledgers**, compared against `e.ledger().sequence()`                                                                                                                                                                                                    | The synthesizer's `spendWindowSecs` must convert seconds → ledgers (≈5 s/ledger; OZ's own example uses 17280 ledgers ≈ 1 day) before it can express an installable param.                                                                                                                                   | `spending_limit.rs:86-95, 222-294`                                                               |
| 12  | Spending limit meters **any outflow of an asset** (repo caps gross outflow per asset across a recorded flow) | ✗ Stock `spending_limit::enforce` ONLY fires on `Context::Contract` with `fn_name == "transfer"`, amount read from `args[2]`; **any other function panics `NotAllowed`** — a router `swap_exact_tokens_for_tokens` call would be rejected outright, not metered. Also asset-blind (one limit per account+rule) and history-capped (`MAX_HISTORY_ENTRIES = 1000`) | **Composing the stock spending-limit policy onto the claim+swap flow does not work.** For the recorded flow, spend-capping the swap requires custom policy logic (fresh Rust) that reads the swap's own args/flows — the compose-first rule's "generate only where stock cannot express it" branch applies. | `spending_limit.rs:222-294`                                                                      |

## Context rules

| #   | Spike assumption                                                                                                                     | Actual OZ reality                                                                                                                                                                     | Impact                                                                                                                                                                                                                                  | OZ source                                                           |
| --- | ------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------- |
| 13  | One context rule can scope **multiple (contract, fn) pairs** (spec's `scopedCalls` lists both the Blend claim and the Soroswap swap) | ✗ `ContextRuleType` is `Default \| CallContract(Address) \| CreateContract(BytesN<32>)` — one rule binds one contract (or all contexts); matching discards the function name entirely | The demo flow needs **two** context rules (one `CallContract` each). `Default` would authorize everything and destroy the least-privilege claim. Spec model + emitter must change.                                                      | `storage.rs:143-150, 289-304`                                       |
| 14  | Function-level scope lives in the context rule                                                                                       | ✗ Rules carry no function name. `enforce` receives the full `Context` (`ContractContext { contract, fn_name, args }`), so function narrowing must be a **policy**                     | The generated policy is the natural home for fn-name (and later argument) narrowing; no stock policy provides it.                                                                                                                       | `storage.rs:289-304`; soroban-sdk `src/auth.rs` (`ContractContext`) |
| 15  | `valid_until` is a **Unix timestamp** (spec emits `now + lifetimeSecs`; summary prints "(unix)")                                     | ✗ `valid_until: Option<u32>` is a **ledger sequence**, compared `valid_until < e.ledger().sequence()`                                                                                 | A Unix value (~1.75e9) installed verbatim ≈ never expires — the opposite of the intended bound. Synthesizer must fetch/estimate the current ledger and convert (≈5 s/ledger), and the simulator's lifetime scenario must model ledgers. | `storage.rs:66, 173, 282, 651, 786`; `mod.rs:21, 203`               |
| 16  | Rule-name cap "20 chars" (`MAX_NAME_SIZE` comment in `src/types.ts`)                                                                 | ✓/nuance: cap is 20 **bytes** (`MAX_NAME_SIZE: u32 = 20`, checked on `String.len()`); `MAX_POLICIES = 5` and `MAX_SIGNERS = 15` confirmed                                             | JS `.length` truncation coincides for the ASCII names generated today; note byte semantics in docs.                                                                                                                                     | `mod.rs:522-530`; `storage.rs:423-427`                              |

**D1.2 status (2026-08-03).** The synthesizer consequences of rows 11–15 are
implemented in the emitted `context-rule.json`
([schema](context-rule-schema.md); committed for the real sequence at
`examples/live/context-rule.json`): one `CallContract` rule per contract with
function names carried only as advisory `observedFns` (rows 13–14);
`valid_until` emitted as a ledger sequence converted at a documented
~5 s/ledger, null + compute-at-install note when the recording has no ledger
(row 15); spend windows converted to `period_ledgers` (row 11); and the stock
`spending_limit` is composed ONLY onto token rules backed by a
subject-authorized direct `transfer` in the recorded authorization tree — the
only context it can meter — with caps it cannot express recorded as `DELTA`
notes instead of emitted as rejectable params (row 12). The token-rule
derivation rests on the verified fact that `__check_auth` receives one
`Context` per `require_auth` call (FACTS §2.5). Regression-locked in
[test/oz-context-rules.test.ts](../test/oz-context-rules.test.ts)
(network-free, including the committed real sequence).

## Recording-layer assumptions (verified against protocol-27 captures)

| #   | Spike assumption                                                                                                              | Actual reality (from committed raw captures)                                                                                                                                                                                                                                                                                                       | Impact                                                                                                                                                                                              | Source                                                       |
| --- | ----------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------ |
| 17  | One `InvokeHostFunction` op per Soroban tx                                                                                    | ✓ Confirmed in all three captures                                                                                                                                                                                                                                                                                                                  | None.                                                                                                                                                                                               | `examples/live/*.json`; FACTS §3.2                           |
| 18  | Envelope is v1 (or fee-bump-wrapping-v1)                                                                                      | ✓ Confirmed — and fee-bump **does occur in the wild** (capture `c857…`)                                                                                                                                                                                                                                                                            | Keep both branches; never assume plain v1.                                                                                                                                                          | FACTS §3.2                                                   |
| 19  | `transfer` events: `topics = [transfer, from, to, ...]`, `data = i128`                                                        | ✓/✗ Split reality: **SAC** transfers have **4 topics** (4th = SEP-0011 asset string, per CAP-67); **plain SEP-41 tokens** still emit **3 topics**. Both coexist in one tx (`2dcff6…`). And per CAP-67, muxed destinations switch `data` from `i128` to `SCV_MAP {amount, to_muxed_id}`                                                             | The rpc.ts matcher tolerates the 4th topic (`...`) but assumes `data` is always `i128` — it must also handle the map form or explicitly reject muxed transfers with a clear error.                  | FACTS §3.3; CAP-67                                           |
| 20  | The scoped call is the top-level invoked function                                                                             | ✗ The only real Blend claim in retention is **nested**: top-level fn is `harvest` on a wrapper contract; the pool's `claim` (event `[claim, claimer]`, data `[Vec<u32>, i128]`) happens a level deeper                                                                                                                                             | A recorder reading only the top-level `InvokeContractArgs` can miss the semantic action being authorized; document as a known T1 limitation or read sub-invocations from the auth entries / events. | FACTS §3.4; capture `acf256…`                                |
| 21  | Blend claim signature `claim(from, reserve_token_ids: Vec<u32>, to)` (fixture args `[addr, [3], addr]`)                       | ✓ Confirmed from blend-contracts-v2 source: `fn claim(e, from: Address, reserve_token_ids: Vec<u32>, to: Address) -> i128`                                                                                                                                                                                                                         | None — fixture arg shape is right.                                                                                                                                                                  | blend-contracts-v2 `pool/src/contract.rs:233-241, 539`       |
| 22  | Soroswap swap signature `swap_exact_tokens_for_tokens(amount_in, amount_out_min, path, to, deadline)`                         | ✓ Confirmed from source AND from a real captured invocation with exactly that arg shape (`i128, i128, Vec<Address>, Address, u64`)                                                                                                                                                                                                                 | None — and `path` at `arg[2]` matches the argument-scope derivation.                                                                                                                                | soroswap/core `router/src/lib.rs:255-262`; capture `2dcff6…` |
| 27  | The subject of token flows is the transaction's **source account** (`rpc.ts` derived it and could only ever produce a `G...`) | ✗ In capture `2dcff6…` the economic actor is the **contract account** `CCW6R5ZK…` (a smart wallet authorizing via `__check_auth`; both transfer legs name it), while the envelope source `GBMMOZMK…` appears in **zero** transfer events — recording that tx attributed no flows at all, precisely for the smart-account case this project targets | **Fixed in D1.1**: the recorder takes an explicit `--account <G\|C>` subject, records it in `RecordedTx.subject`, and when defaulted to the source account says so in `RecordedTx.warnings`.        | capture `2dcff6…`; FACTS §3.3; src/sources/decode.ts         |

**D1.1 status (2026-08-03).** The recorder consequences of rows 17–22 and 27
are implemented: multi-hash `record <hash...>` merging one `RecordedTx` in
ledger-close-time order (row 17 makes multi-hash structurally necessary);
fee-bump and v1 envelopes (row 18); both transfer topic shapes plus the CAP-67
`{amount, to_muxed_id}` map data, and SAC `mint`/`burn`/`clawback`, with
undecodable movement-like events surfaced as warnings, never silently skipped
(row 19); the authorization-entry tree decoded into each call's
`authorizations` (row 20) — note the `acf256…` claim's auth tree is a bare
`harvest` with no sub-invocations, so the nested pool claim is still only
visible via its _event_; movements are event-derived, so no value is missed,
but call-scope derivation from a wrapped invocation remains a documented T1
limitation. Everything is regression-locked against the committed captures in
[test/recorder.test.ts](../test/recorder.test.ts) (network-free).

## Toolchain and venue assumptions

| #   | Spike assumption                                                         | Actual reality                                                                                                                                                                                                                                                | Impact                                                                                                                                                                                                                                                                     | Source                                                      |
| --- | ------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------- |
| 23  | Wasm target is `wasm32-unknown-unknown`                                  | ✗ `stellar contract build` (26.0.0 and 27.1.0) targets **`wasm32v1-none`** for rustc ≥1.84 (1.82–1.84 and exactly 1.91.0 are rejected); the old target only for rustc <1.82                                                                                   | Any build docs/scripts must say `wasm32v1-none`; target is already installed locally.                                                                                                                                                                                      | FACTS §1.3; stellar-cli v27.1.0 `build.rs:209-210, 718-740` |
| 24  | `stellar contract build --verifiable` exists (digest-pinned image)       | ✗ **No such flag** in 26.0.0 or 27.1.0 (0 hits in the full v27.1.0 help corpus). Closest: automatic `--remap-path-prefix` for reproducibility + warnings when RUSTFLAGS break it                                                                              | Drop any "verifiable build" claim from plans; record reproducibility as path-remapping only.                                                                                                                                                                               | FACTS §1.2                                                  |
| 25  | soroban-sdk to pin = latest (27.0.4)                                     | ✗ OZ v0.7.2 pins **soroban-sdk 26.1.0**; compiling generated policies against OZ v0.7.2 means using 26.1.0                                                                                                                                                    | Pin 26.1.0 in generated Cargo.toml until OZ ships a stable on sdk 27.                                                                                                                                                                                                      | OZ workspace `Cargo.toml:55`; FACTS §1.1                    |
| 26  | Fixture's Soroswap/Blend/token contract IDs are real testnet deployments | ✗ All six fixture addresses are synthetic placeholders (valid StrKey, no on-chain instance). Real, verified-on-chain testnet IDs now recorded: Blend BLND/USDC mints, Comet BLND:USDC pool (deep liquidity), Soroswap router/factory/pair (shallow liquidity) | Fixture stays clearly labeled synthetic; live walkthrough uses the FACTS §4 IDs. Venue chosen: **Soroswap router** (matches the modeled call shape; small swaps only), Comet `swap_exact_amount_in` as the deep-liquidity fallback (different call shape — no `path` arg). | FACTS §4.1–4.4                                              |

## Summary of required changes (Prompts 2–3)

Derived from the ✗ rows, in dependency order:

1. **Spec model** (rows 13–15): split `ContextRule.scopedCalls` into one
   `CallContract` rule per contract; move fn-name scope into the generated
   policy; emit `valid_until` as a ledger sequence (seconds → ledgers
   conversion with a documented ~5 s/ledger estimate or a live
   `getLatestLedger` read). — **DONE in D1.2** (2026-08-03; see the status
   note under the context-rules table). Fn-name scope is emitted as advisory
   `observedFns`; enforcing it in generated policy code is T2.
2. **Policy synthesis** (rows 11–12): convert spend windows to
   `period_ledgers`; stop presenting the stock `spending_limit` as composable
   over the swap flow — the swap spend cap must live in generated Rust (the
   stock module still fits a plain-`transfer` rule). — **DONE in D1.2**,
   refined by FACTS §2.5: the recorded swap's authorization tree contains a
   subject-authorized direct `transfer` leg, and since every `require_auth`
   is its own context, that leg gets its own `CallContract(token)` rule where
   the stock `spending_limit` composes and meters the actual outflow. Caps
   without such a leg are emitted as `DELTA` notes, not installable params.
3. **Generated Rust** (rows 5, 7): add the `AlreadyInstalled` install guard,
   panic-if-not-installed `uninstall`, and TTL extension constants to match
   the stock pattern.
4. **Recorder** (rows 19–20, 27): ~~handle CAP-67 map-shaped `transfer` data;
   decide and document the nested-invocation stance~~ — **DONE in D1.1**
   (2026-08-03, see the status note under the recording-layer table).
5. **Docs/venue** (rows 23–26): `wasm32v1-none`, no `--verifiable`, sdk pin
   26.1.0, Soroswap-first venue with real IDs.
