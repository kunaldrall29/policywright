# Freighter preferred-path attempt — 2026-09-17

## Goal
Preferred install signing via Freighter `signAuthEntry` (FACTS §5.3) against
`wallet/` + live testnet plan for smart account
`CALCGK5RRRVOV5XUGRUPX3NT5XZF3XUDL3SHM7ZXZEPNFMLFGJNCTV5W`.

## What worked
- Unpacked Freighter **v5.48.0** loaded in Chrome (`tools/freighter-extension/`, gitignored).
- Wallet onboarding + seed import completed; extension enabled.
- Local wallet UI served at `http://127.0.0.1:4173/` (HTTP 200).
- Install plan ready (`readyToSign: true`); expected G-signer
  `GCODZM35QUK7HD4JODIBMRTK7DOTF2AVNIPPYIYYPRJFJCNH6CMWVZAK`.

## What blocked preferred signing
- `@stellar/freighter-api` `isConnected()` never became true on the page.
- Console: content script not injecting `FREIGHTER_EXTERNAL_MSG_*` bridge into
  `http://127.0.0.1` when the extension is loaded unpacked (MV3 service worker
  often inactive; `window` has no Freighter bridge).
- Tried freighter-api 3.0 / 3.1 / 4.1 / 5.0 — same detection failure.
- **No Freighter approval popup; no Freighter-signed tx.**

## Honest status
| Path | Status |
|------|--------|
| Freighter interactive `signAuthEntry` | **BLOCKED-honest** (extension↔page bridge) |
| Local-signer install + live verify | **Green** (see EVIDENCE D2.5 + CALCGK5 install this session) |

Screenshots: `freighter-onboarding-success.png`,
`wallet-page-freighter-detection-failure.png`,
`freighter-api-undefined-console.png`,
`freighter-blocker-final-state.png`.

Secrets were never written to git-tracked files.
